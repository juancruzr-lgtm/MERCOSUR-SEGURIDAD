# Dependencias (se regeneran con npm install)
node_modules/

# Build de Next.js (se regenera con npm run build/dev)
.next/

# Variables de entorno con claves reales — NUNCA commitear
.env.local
.env*.local

# Artefactos varios
*.tsbuildinfo

# Auditorias locales con credenciales
audit*_temp.js

# Planillas privadas de clientes
planiallas para comparar/
